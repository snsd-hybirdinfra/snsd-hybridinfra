diagram:
  type: relationship-overview
  title: Kubernetes Cluster Health Monitoring - relationship-overview

  scenario: kubernetes-cluster-health-monitoring
  lifecycle: level-1-visibility
  layout: left-to-right

nodes:
  - id: kubernetes-cluster
    label: Kubernetes Cluster
    type: cluster
    group: platform
  - id: worker-node
    label: Worker Node
    type: compute
    group: platform
  - id: pod-workload
    label: Pod Workload
    type: workload
    group: platform
  - id: cluster-telemetry
    label: Cluster Telemetry
    type: telemetry
    group: observability
  - id: operations-dashboard
    label: Operations Dashboard
    type: dashboard
    group: observability
edges:
  - from: kubernetes-cluster
    to: worker-node
    label: schedules workloads
  - from: worker-node
    to: pod-workload
    label: runs pods
  - from: pod-workload
    to: cluster-telemetry
    label: emits runtime signals
  - from: cluster-telemetry
    to: operations-dashboard
    label: publishes visibility
