scenario_name: compute-failover-orchestration
scenario_title: Compute Failover Orchestration
lifecycle_level: level-3-recovery
scenario_path: scenarios/level-3-recovery/compute-failover-orchestration
scenario_type: Recovery / Automation
primary_domain: Compute / Resource
status: content-filled
overview: |
  This scenario documents compute failover orchestration within the compute / resource operational
  domain. It focuses on compute node, instance, CPU, memory, disk, runtime host and demonstrates how
  infrastructure operations teams can use domain-specific telemetry, lifecycle workflow design, and
  evidence-backed validation to support execute controlled recovery, restoration, failover, or
  mitigation workflow.

objectives:
  - Define the scenario-specific compute / resource signal represented by compute-failover-orchestration.
  - Identify the affected compute / resource components and dependencies.
  - Collect and interpret telemetry from compute node, instance, CPU, memory, disk, runtime host.
  - Use CPU usage as an operational signal for detection or validation.
  - Use memory usage as an operational signal for detection or validation.
  - Use disk pressure as an operational signal for detection or validation.
  - Document the lifecycle workflow from detection through validation.
  - Produce reviewer-readable evidence artifacts for portfolio assessment.

operational_focus: |
  Execute controlled recovery, restoration, failover, or mitigation workflow

target_resource: |
  compute node, instance, CPU, memory, disk, runtime host

telemetry_signals:
  - CPU usage
  - memory usage
  - disk pressure
  - instance status
  - host health
  - reboot event
  - availability state

infrastructure_components:
  - Compute Node
  - Instance
  - Cpu
  - Memory
  - Disk
  - Runtime Host
  - Telemetry Source
  - Detection Logic
  - Evidence Output

detection_workflow: |
  CPU usage; memory usage; disk pressure; instance status; host health; reboot event; availability
  state

correlation_and_analysis: |
  Correlate compute / resource signals with related infrastructure state, dependencies, recent events,
  and service impact.

alert_and_incident_workflow: |
  Execute controlled recovery, restoration, failover, or mitigation workflow

recovery_and_automation_workflow: |
  Execute controlled recovery, restoration, failover, or mitigation workflow

recovery_validation: |
  Validate stable state, evidence completeness, and operational readiness after detection, analysis,
  response, or recovery.

monitoring_and_visibility: |
  Monitoring and visibility include CPU usage; memory usage; disk pressure; instance status; host
  health; reboot event; availability state.

used_modules:
  - Recovery Orchestration Module
  - Automation Execution Module
  - Recovery Validation Module

used_adapters:
  - Ansible Adapter
  - Prometheus Adapter
  - Grafana Adapter

operational_components:
  - name: Compute Node
    purpose: Provides context or signal source for Compute / Resource operations
  - name: Instance
    purpose: Provides context or signal source for Compute / Resource operations
  - name: Cpu
    purpose: Provides context or signal source for Compute / Resource operations
  - name: Memory
    purpose: Provides context or signal source for Compute / Resource operations
  - name: Disk
    purpose: Provides context or signal source for Compute / Resource operations
  - name: Runtime Host
    purpose: Provides context or signal source for Compute / Resource operations
  - name: Telemetry Source
    purpose: Provides context or signal source for Compute / Resource operations
  - name: Detection Logic
    purpose: Provides context or signal source for Compute / Resource operations
  - name: Evidence Output
    purpose: Provides context or signal source for Compute / Resource operations
  - name: Correlation Logic
    purpose: Connects related signals, dependencies, and impact context
  - name: Validation Method
    purpose: Confirms stable state, restored condition, or visibility completeness

evidence_outputs:
  - summary.md
  - execution-evidence.md
  - validation-evidence.md
  - artifact-manifest.json
  - artifact-checksums.json

expected_outcomes:
  - The scenario has domain-specific operational context.
  - Telemetry signals are identified and mapped to the scenario purpose.
  - Infrastructure components and dependencies are documented.
  - Lifecycle workflow sections are populated with scenario-specific content.
  - Validation and evidence outputs are defined for portfolio review.

validation_checklist:
  - Scenario metadata is present.
  - Operational poster reference is preserved.
  - Used modules are listed.
  - Used adapters are listed.
  - Detection workflow is scenario-specific.
  - Correlation and analysis workflow is scenario-specific.
  - Response or recovery workflow is described.
  - Recovery validation is described.
  - Evidence links are present.
  - Deprecated diagram references are not used.

related_scenarios:
  upstream: []
  same_level: []
  downstream: []
  cross_domain: []
